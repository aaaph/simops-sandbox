# Build context of every service simops generates (the repo root).
# The macOS pixi env alone is ~5 GB and useless in a Linux image.
.pixi/
.git/
.stack/
.vscode/
**/__pycache__/
.pytest_cache/
.ruff_cache/
**/.DS_Store
build/
